export const CLI_OPTION_FLAG_PATTERN = /--?[A-Za-z][A-Za-z0-9-]*/g;
export const CLI_OPTION_VALUE_PATTERN = /[<[]/;
export const CLI_PROMPT_MASK = '*';
export const CLI_HELP_FLAGS = ['--help', '-h', 'help'];
export const CLI_COLUMN_GAP = 2;
export const CLI_PASSWORD_PROMPT = 'Password for the new user (they will be asked to change it on first sign-in): ';
